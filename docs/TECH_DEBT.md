# Known compromises / tech debt

One line per shortcut, so it's a visible backlog instead of a forgotten
surprise. Add to this whenever you cut a corner under deadline pressure.

| Date       | Area     | Compromise                                                                                                                                                                                                                                                                                                                                                                                                     | Follow-up needed                                                                                                                                                                                                                               |
| ---------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-30 | LMS      | Course completion is derived from `ModuleProgress` rows, but whether a learner actually watched a module is still the client's assertion — nothing verifies playback. Ownership, expiry and module-belongs-to-course are enforced, so a learner can only over-report their own progress, not anyone else's.                                                                                                    | Drive `POST /courses/enrollments/:id/modules/:moduleId/complete` from Cloudflare Stream playback events instead of client calls, once Stream is provisioned.                                                                                   |
| 2026-08-01 | LMS      | **Reversal of the line above.** A "Mark lesson as watched" button now exists in `LessonPlayer`. Gated assessments made the old position untenable: with no Stream integration and no button, no learner could ever reach a test, so the entire quiz-and-certificate flow was unreachable. Self-certification is now one click, exactly as warned.                                                              | Delete the button the day Stream is provisioned and let playback events drive completion. Until then this is a demo affordance, and any certificate issued in this window attests to quiz scores only — not to anyone having watched anything. |
| 2026-08-07 | Admin    | **Role.SUPPORT can be authorised for data it has no screen to reach.** The API grants SUPPORT read access to the order list and the user list, but `STAFF_ROLES` in `shared/lib/staff.ts` excludes it, so a support agent signing into the panel is told customer accounts can't sign in here. Long-standing, not introduced by the session-restore work — surfaced by writing the first test for that policy. | Decide whether SUPPORT belongs in the panel. If it does, add it to `STAFF_ROLES` and hide the actions it isn't authorised for (refunds, role changes); if it doesn't, narrow the API's @Roles to match. Either way the two should agree.       |
| 2026-09-25 | Payments | ~~A refund does not produce a credit note, and does not revoke the entitlement.~~ **Both done.** A refund now issues a numbered credit note in its own CRN series and takes back what the payment granted — the enrolment expires, an undownloaded document is blocked, a queued review is cancelled. A content item needs nothing: the PAID order is the entitlement.                                         | Buyer GSTIN still isn't collected, so a B2B credit note can't carry one — same dependency as the invoice row below.                                                                                                                            |
| 2026-08-01 | Payments | **No buyer address is collected, so place of supply always falls back to the supplier's state** and every sale is billed as intra-state CGST+SGST. This is the statutory fallback (IGST Act s12(2)(b)) rather than a shortcut, so it is correct today — but it silently stops being correct the moment an address field exists and is not wired through.                                                       | Add address to the customer profile (docs/srs.md 3.1 already calls for it), then pass the state to `resolvePlaceOfSupply`. The split logic and the stored `placeOfSupply` column already handle inter-state; nothing else changes.             |
| 2026-08-01 | Payments | SAC codes per item type are a developer's best reading of what we sell, not an accountant's ruling. Wrong codes are a filing problem, not a customer-visible one.                                                                                                                                                                                                                                              | Have the client's CA confirm the four codes in `invoice-config.ts`; each is already overridable by env so no deploy is needed to correct them.                                                                                                 |
| 2026-08-01 | Payments | The invoice is not emailed. `docs/srs.md` 3.5 wants it attached to the purchase confirmation, but no purchase-confirmation email exists at all yet and `NotificationsService.sendEmail` takes HTML with no attachment support.                                                                                                                                                                                 | Add the confirmation email with a link to the dashboard rather than an attached PDF — a signed download URL expires in five minutes and would be dead before most people opened the mail.                                                      |
| 2026-08-01 | LMS      | Quiz answers are saved per click, so a test with 20 questions is 20 requests. Fine at current scale, and it's what makes a timeout cost only the blanks.                                                                                                                                                                                                                                                       | Batch with a debounce if it ever shows up in the API's request volume. Don't move to a single submit-at-the-end post — that trades a real guarantee for a saving nobody asked for.                                                             |
| 2026-07-30 | Storage  | `StorageService.signUrl` streams downloads through the API in both local and R2 mode rather than issuing R2 presigned URLs. Correct, but puts our bandwidth in the path of every download.                                                                                                                                                                                                                     | Swap to `@aws-sdk/s3-request-presigner` once R2 is provisioned. Purely an optimisation — the signature check stays either way.                                                                                                                 |
| 2026-07-30 | Payments | `GST_RATE` is duplicated: authoritative in `PaymentsService`, mirrored in `@pratikar/utils` so catalogue pages can quote a total before an order exists.                                                                                                                                                                                                                                                       | Collapse to one source if the rate ever becomes configurable, and confirm the actual applicable rate with the client's accountant (open blocker 5).                                                                                            |
