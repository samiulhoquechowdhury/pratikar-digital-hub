// Two-step form: identifier (email/phone) -> OTP code.
// Intentionally not implemented yet — placeholder for Milestone 0's first
// real vertical slice (Auth). Build this once useOtpFlow is wired up.
export function OtpForm() {
  return null;
}
