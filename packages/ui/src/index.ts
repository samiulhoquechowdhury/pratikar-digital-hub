// Shared design-system components (per the brand/design system doc: Deep Navy/Royal
// Blue/Cyan palette) used by both apps/web and apps/admin, so the two Next.js apps
// never drift visually. Start populating this once the first real screen is built —
// don't pre-build a component library speculatively.
export {};
